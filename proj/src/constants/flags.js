// 기능 플래그 — 코드는 보존하고 표시 여부만 토글한다.
//
// SHOW_KPI_TARGETS: 사고절감 KPI 목표 표기(대시보드 목표대비 배지 + 헤더 "KPI 목표" 버튼/패널).
//   목표치가 아직 조직 합의 전(전년×0.9 자동 프리필)이라 표기는 시기상조 → 현재 OFF.
//   목표 확정 후 true 로 바꾸면 배지·관리자 패널이 그대로 되살아난다.
export const SHOW_KPI_TARGETS = false;
