// === 산재 승인 현황 데이터 ===
// DB/산재승인현황.xlsx 배치 후 `cd proj && node scripts/extract-approval.mjs` 실행하면 자동 갱신.
// 2차: HR API 자동 연동 예정.

const APPROVAL_DATA = null; // Excel 연동 전 — null 이면 UI가 "연동 대기" 표시
export default APPROVAL_DATA;
